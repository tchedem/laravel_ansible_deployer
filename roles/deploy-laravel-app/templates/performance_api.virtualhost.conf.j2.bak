# HTTPS configurations
server {
    listen                  443 ssl http2;
    listen                  [::]:443 ssl http2;
    server_name             {{ laravel_app1_name_domain_name }};
    root {{ laravel_app1_path }}/public;
    index index.php;

    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";
    # add_header Permissions-Policy "interest-cohort=()";
    # add_header Permissions-Policy "geolocation=(), microphone=(), camera=()";
    client_max_body_size 0;

    charset utf-8;

    # index.php fallback
    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    # Runtime to use with every request URI end with .php
    location ~ \.php$ {

        # Timeout in current runtime
        proxy_read_timeout 10s;
        proxy_connect_timeout 10s;
        proxy_send_timeout 10s;
        fastcgi_send_timeout 10s;
        fastcgi_read_timeout 10s;

        #fastcgi_pass unix:/var/run/php/php8.1-fpm.sock;
        fastcgi_pass unix:/var/run/php/php8.1-fpm-api_test.sock;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;

    }

    # You can Customize error pages
    error_page 404 /index.php;

    # security
    # include                 nginxconfig.io/security.conf;

    # # SSL (Generated by certbot)
    #ssl_certificate         /etc/nginx/ssl/{{ laravel_app1_name_domain_name }}.crt;
    #ssl_certificate_key     /etc/nginx/ssl/{{ laravel_app1_name_domain_name }}.key;
    
    # ssl_trusted_certificate /etc/letsencrypt/live/api.{{ laravel_app1_name_domain_name }}/chain.pem;
    # ssl_certificate         /etc/letsencrypt/live/api.{{ laravel_app1_name_domain_name }}/fullchain.pem;
    # ssl_certificate_key     /etc/letsencrypt/live/api.{{ laravel_app1_name_domain_name }}/privkey.pem;
    # ssl_trusted_certificate /etc/letsencrypt/live/api.{{ laravel_app1_name_domain_name }}/chain.pem;

    access_log /var/log/nginx/{{ laravel_app1_name_domain_name }}.log;
    error_log /var/log/nginx/{{ laravel_app1_name_domain_name }}.err;

}

# HTTP redirect
server {
    listen      80;
    listen      [::]:80;
    server_name {{ laravel_app1_name_domain_name }};

    location / {
        return 301 https://{{ laravel_app1_name_domain_name }}$request_uri;
    }
}

{# 
{% raw %}
server {
    listen 80;
    server_name santerecherchepro.org;

    root /home/vagrant/laravel_app1_name/public;
    index index.php index.html;

    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";

    charset utf-8;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/php8.2-fpm.sock; # adjust PHP version if needed
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
    }

    location ~ /\.ht {
        deny all;
    }
}

{% endraw %}
#}