server {
    listen      80;
    listen      [::]:80;
    server_name {{ PROJECT_DOMAIN_NAME }};
    root {{ PROJECT_PATH }}/public;
    index index.php;

    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";
    client_max_body_size 0;

    charset utf-8;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        # fastcgi_pass unix:/var/run/php/php8.2-fpm.sock;
        fastcgi_pass unix:/run/php/php8.2-fpm-{{ APP_NAME }}.sock;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
    }

    # WebSocket proxy for GoAccess real-time updates
    location /ws/ {
        proxy_pass http://127.0.0.1:7890;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "Upgrade";
        proxy_set_header Host $host;
    }

    error_page 404 /index.php;

    access_log /var/log/nginx/{{ PROJECT_DOMAIN_NAME }}.log;
    error_log /var/log/nginx/{{ PROJECT_DOMAIN_NAME }}.err;
}
